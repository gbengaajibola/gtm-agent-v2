.env
.venv/
__pycache__/
*.pyc
output/*.png
# Local review copies — spike plan/eval, never pushed
PLAN_STAGE4_OPENAI_SDK.md
EVAL_STAGE4_OPENAI_SDK.md
# NOTE: .opencode/ (subagent routing) and opencode.json are intentionally
# TRACKED — every clone needs them for @planner/@executor/@evaluator to load.
